/**
 * Manually update this version number to force browsers to reload cached assets.
 * A good practice is to increment this after making significant changes before deployment.
 * Example: '1.0.1' -> '1.0.2'
 */
export const APP_VERSION = '1.0.1';
